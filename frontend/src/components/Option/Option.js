import React from 'react'
import { observer } from "mobx-react"

const Option = () => {
    return (
        <div className = 'OptionRoot'>
            
        </div>
    )
}
export default (observer(Option))